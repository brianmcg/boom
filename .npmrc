# Save exact versions, never a caret range.
#
# pixi.js 8.17 changed BindGroup.onResourceChange so that destroying a texture
# destroys the whole bind group holding it, which crashes the game two scene
# transitions later. A `^` range would let that back in on any fresh install.
# See the pixi note in CLAUDE.md.
save-exact=true
