/**
 * The hex value for white.
 * @type {Number}
 */
export const WHITE = 0xffffff;

/**
 * The hex value for black.
 * @type {Number}
 */
export const BLACK = 0x000000;

export const FONT = {
  SMALL: `${SCREEN_HEIGHT / 15}px font`,
  MEDIUM: `${SCREEN_HEIGHT / 10}px font`,
  LARGE: `${SCREEN_HEIGHT / 5}px font`
};
