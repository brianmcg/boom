class Ray {
  constructor({
    startPoint,
    endPoint,
    distance,
  }) {
    this.startPoint = startPoint;
    this.endPoint = endPoint;
    this.distance = distance;
  }
}

export default Ray;
